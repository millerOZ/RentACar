@model IEnumerable<RentACar.Data.Entities.Vehicle>

@{
    ViewData["Title"] = "Index";
}

<flash dismissable="true" />

<!-- Content Wrapper. Contains page content -->
<div class="container-fluid">
    <div class="card mt-2">
        <div class="card-header text-danger">
            <i class="fa fa-car"></i>  <strong>Vehiculos</strong>
        </div>
        <div class="card-body">
            <div id="view-all">
                @await Html.PartialAsync("_ViewAllVehicles", Model)
            </div>
        </div>
    </div>
</div>

<!--modal placeholder. Also you can place this modal form in the _Layout.cshtml-->
<div class="modal fade in" tabindex="-1" role="dialog" data-bs-backdrop="static" id="form-modal">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header bg-primary">
                <h5 class="modal-title text-white"></h5>
                <button type="button" class="btn-close text-white" data-bs-dismiss="modal" aria-label="Close">                   
                </button>
            </div>
            <div class="modal-body">
            </div>
        </div>
    </div>
</div>

<partial name="_DeleteDialog" />
<partial name="_AddOrEditDialog" />

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}

    <script src="~/js/showmodal.js"></script>

    <script type="text/javascript">
        $(document).ready(function () {
            $('#ProductsTable').DataTable({
                "language": {
                    "url": "//cdn.datatables.net/plug-ins/9dcbecd42ad/i18n/Spanish.json"
                },
                "aLengthMenu": [
                    [25, 50, 100, 200, -1],
                    [25, 50, 100, 200, "Todos"]
                ]
            });
        });
    </script>

    <script>
        sc_deleteDialog.openModal('deleteItem', true, 'btnYesDelete', '/Products/Delete/', false);
    </script>
}



@*@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}

    <script src="~/js/showmodal.js"></script>

    <script type="text/javascript">
        $(document).ready(function () {
            $('#VehicleTable').DataTable({
                "language": {
                    "url": "//cdn.datatables.net/plug-ins/9dcbecd42ad/i18n/Spanish.json"
                },
                "aLengthMenu": [
                    [25, 50, 100, 200, -1],
                    [25, 50, 100, 200, "Todos"]
                ]
            });
        });
    </script>

    <script>
        sc_deleteDialog.openModal('deleteItem', true, 'btnYesDelete', '/Vehicle/Delete/', false);
    </script>
}*@



@*<p>
    <a asp-action="Create" class="btn btn-outline-primary">Crear Vehículo</a>
</p>
<div class="row">
    <div class="col-md-12">
    <div class="panel panel-default">
    <div class="panel-heading">
    <h3 class="panel-title">Vehículos</h3>
    </div>
    <div class="panel-body">
    <table class="table table-hover table-responsive table-striped" id="MyTable">
    <thead>
    <tr>
    <th>
    @Html.DisplayNameFor(model => model.Plaque)
    </th>
    <th>
    @Html.DisplayNameFor(model => model.Brand)
    </th>
    <th>
    @Html.DisplayNameFor(model => model.Serie)
    </th>
    <th>
    @Html.DisplayNameFor(model => model.Description)
    </th>
    <th>
    @Html.DisplayNameFor(model => model.CategoriesNumber)
    </th>
    <th>
    @Html.DisplayNameFor(model => model.ImagesNumber)
    </th>
    <th>
    @Html.DisplayNameFor(model => model.ImageFullPath)
    </th>
    <th></th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
    <tr>
    <td>
    @Html.DisplayFor(modelItem => item.Plaque)
    </td>
    <td>
    @Html.DisplayFor(modelItem => item.Brand)
    </td>
    <td>
    @Html.DisplayFor(modelItem => item.Serie)
    </td>
    <td>
    @Html.DisplayFor(modelItem => item.Description)
    </td>
    <td>
    @Html.DisplayFor(modelItem => item.CategoriesNumber)
    </td>
    <td>
    @Html.DisplayFor(modelItem => item.ImagesNumber)
    </td>
    <td>
    <img src="@item.ImageFullPath" style="width:100px;" />
    </td>
    <td>
    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-outline-warning">Editar</a>
    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-outline-info">Detalles</a>
    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-outline-danger">Borrar</a>
    </td>
    </tr>
    }
    </tbody>
    </table>
    </div>
    </div>
    </div>
    </div>

    @section Scripts {
    @{
    await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
     

    <script type="text/javascript">
    $(document).ready(function () {
    $('#MyTable').DataTable({
    "language": {
    "url": "//cdn.datatables.net/plug-ins/9dcbecd42ad/i18n/Spanish.json"
    },
    "aLengthMenu": [
    [25, 50, 100, 200, -1],
    [25, 50, 100, 200, "Todos"]
    ]
    });
    });
    </script>
    }*@


@*<div class="row row-cols-1 row-cols-md-4 g-4 mt-1">
    @if (Model.Count() > 0)
    {
        @foreach (var item in Model)
        {
            <div class="col">
                <div class="card bg-light h-100">
                    <h5 class="card-header text-center"><i class="fa-solid fa-user-gear"></i>@Html.DisplayFor(modelItem => item.Plaque)</h5>
                    <div class="card-body bg-info bg-gradient p-2 text-dark bg-opacity-50">
                        <div class="text-center zoom">
                            <img src="@item.ImageFullPath" style="width:100px; height:100px; border-radius:50px" class="text-center" />
                        </div>
                        <div class="card-body text">
                            <p class="card-text smfnt">Serie:   @item.Serie</p>
                            <p class="card-text smfnt">Marca:   @item.Brand</p>
                            <p class="card-text smfnt">Valor Diario:   @item.PriceDay</p>
                            <p class="card-text smfnt">Descripción:    @item.Description</p>
                            <p class="card-text smfnt">Categorías:    @item.CategoriesNumber</p>
                            <p class="card-text smfnt">Fotos:    @item.ImagesNumber</p>
                        </div>
                        <div class="col mt-3 text-center">
                             <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-dark">Editar</a>
                            <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-dark">Detalles</a>
                            <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-dark">Borrar</a>
                        </div>

                        <div class="card-footer text-center">
                        </div>
                    </div>
                </div>
            </div>

        }
    }
    else
    {
        <h3>No hay usuarios registrados</h3>
    }
</div>
*@
